%table
  %tr
    %th Name
    %th Attending
    %th Food
  
  -current_family.guests.each do |guest|
    %tr
      %td= guest.name
      %td= guest.rsvp ? "Yes please" : "No sorry I can't make it"
      %td= guest.dietary
      
  
%p= link_to "Edit RSVP", edit_rsvp_path
