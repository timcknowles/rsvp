= simple_form_for @guest, { :class => 'form-horizontal'} do |f|
  
  = f.input :first_name
  = f.input :last_name
  = f.input :invite_type
  = f.input :rsvp


  
  = f.association :family

  .form-actions
    = f.button :submit