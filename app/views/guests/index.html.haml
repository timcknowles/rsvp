%h1 Listing guests

%table
  %tr
    %th First name
    %th Last name
    %th Family Name
    %th Invite type
    %th Email
    %th
    %th
    %th

  - @guests.each do |guest|
    %tr
      %td= guest.first_name
      %td= guest.last_name
      %td= guest.family.name
      %td= guest.invite_type
      %td= guest.email
      %td= link_to 'Show', guest
      %td= link_to 'Edit', 
      %td= link_to 'Destroy', guest, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Guest', new_guest_path
