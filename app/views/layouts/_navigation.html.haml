
%ul.nav
  - if user_logged_in?
    %li.home-page-menu-item 
      = link_to 'Logout',logout_path(@session)
   

     
             
       
    
  - else
    %li.home-page-menu-item
      = link_to 'Login', login_path(@session)   
    %li.home-page-menu-item
      = link_to'Create Invite', signup_path(@guest)

    %li.home-page-menu-item
      = link_to 'Add guests to families', new_guest_path(@guest)

    %li.home-page-menu-item
      = link_to 'Guestlist', guests_path(@guest)


